# Player 1.9.3 시험 보고서

시험일: 2026-09-11. Linux / Node.js 24.19.0. 웹 Player 전체 소스 대상.

## 최종 결과

- 전체 자동 회귀 시험: 69개 통과, 실패 0, 취소 0, 건너뜀 0.
- JS/CJS 문법 검사: 10개 파일 통과.
- 왼쪽 waiting-left.jpg: IMG_9381.jpeg와 원본 바이트 동일, 1399×1124.
- 오른쪽 waiting-right.jpg: IMG_9380.jpeg와 원본 바이트 동일, 819×1536.
- 이미지 디코딩 확인, HTML 참조 파일 존재 및 서비스 워커 이미지 포함 여부 검사 통과.

최종 원시 로그는 verification/player193-final.tap과 verification/player193-static.json입니다.

## 새로 확인한 동작

- 좌우 준비 화면 및 영상 poster가 각각 다른 지정 이미지를 사용합니다.
- 영상 준비 시 직전 플레이리스트 이미지가 남아 있어도 해당 칸의 대기 이미지로 전환합니다.
- 재생 대기 중 영상은 투명하고 컨트롤은 비활성화되어 있으며 아래의 지정 이미지가 유지됩니다.
- 재생 시작과 현재 데이터가 확인되면 영상이 표시됩니다. 데이터가 없으면 재생 알림만으로 표시하지 않습니다.
- 자동 재생 거부 시 기존 제한된 재시도/복구 흐름을 유지합니다.
- 이전 영상의 늦은 콜백이 교체된 영상의 표시를 바꾸지 않습니다.

기존 좌우 재생, 디코더 수 제한, 파일 검증·복구, 저장 목록, 일정 및 서비스 워커 리디렉션 회귀 시험을 함께 수행했습니다.

## 시험 한계와 현장 확인

Node VM과 가짜 DOM·미디어·캐시를 사용했습니다. 실제 Android/Chrome 네이티브 포스터 및 CSS 렌더링 결과를 TV에서 검증한 것은 아닙니다. 포함된 72시간 시험은 가짜 시계로 가속한 시험이며 실제 기기 72시간 운전이 아닙니다.

현장에서는 1.9.3 적용 후 첫 시작, 이미지→영상, 영상→영상, 좌우 동시 영상에서 각 대기 이미지와 전환을 확인하세요. 한쪽이 준비되는 동안 반대쪽의 정상 송출도 확인해야 합니다.

CMS와 APK, 원본 영상은 변경하지 않았습니다. 실제 운영 서버에는 자동 배포하지 않았습니다.

## 재현

Node.js 24로 프로젝트 루트에서 실행합니다. 추가 의존성은 없습니다.

```sh
node --test --test-reporter=tap --test-concurrency=1 tests/*.test.cjs
```

오류 주입 시험에서 표시되는 오류 메시지와 최종 실패 건수를 구분해서 읽어 주세요.
