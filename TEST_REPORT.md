# LocalVision Player 1.9.0 / CMS 2.1.0 — 검증 보고서

검증일: 2026-09-10. 기준 파일: 이전 전달본 Player 1.8.1 / CMS 2.0.6.

| 검증 항목 | 결과 |
|---|---|
| Player 앱 통합·오프라인 재시작·스케줄·적용 기록 | 12개 통과 |
| 파일 SHA-256·캐시·목록 저장 기록 | 13개 통과 |
| 좌우 재생 엔진·오류 큐·72시간 가속 시뮬레이션 | 21개 통과 |
| 서비스 워커 설치·버전 일치 | 2개 통과 |
| CMS 기존 API·SQLite DB | 12개 통과 |
| CMS 원본 검증 정보·업로드·시간대·상태 보고 | 8개 통과 |
| CMS MP4 기본 검사 | 6개 통과 |
| CMS 진단 UI 로직 | 4개 통과 |
| JavaScript 구문·정적 파일 참조 | 86개 JS 구문 통과, HTML/SW 참조 누락 없음 |
| CMS 빌드 | dist 재빌드 및 원본과 바이트 일치 |

**최종 구성 78개 시험을 두 차례 완주하여 각각 78개 통과, 실패 0개, 건너뜀 0개를 확인했습니다.** verification 폴더에 프로젝트별 실제 출력 두 회분을 포함합니다. npm test에 기재된 Node 시험 명령으로 재실행할 수 있습니다.

## 주요 장애 주입
- 정상 원본과 크기는 같지만 내용이 다른 파일, 중간이 잘린 파일, 잘못된 해시/리비전 정보를 거절합니다.
- 손상 캐시를 해당 파일만 복구하며, 잘못된 원본은 두 번까지만 시도하고 새 캐시에 등록하지 않습니다.
- 같은 파일의 다른 리비전이 이전 캐시를 덮어쓰지 않고, 현재 검증 완료 파일은 사전 다운로드 대기열을 우회합니다.
- 로컬 저장소 할당 실패는 새 목록 적용을 막고 이전 적용 기록·좌우 목록을 유지합니다.
- 다음 시간대 목록 준비 후 오프라인 전환, 현재 파일 누락 시 이전 저장본 선택, 새 VM 앱 실행 환경에서 오프라인 복원을 검증합니다.
- 좌측 목록만 바꾸면 우측 재생 세대가 변경되지 않습니다.
- 신규 업로드는 콘텐츠 DB 행이 공개되기 전에 검증 정보가 저장되고, 실제 CMS snapshot 응답에 동일 리비전이 전달됩니다.
- 기존 파일 검증 정보 생성은 파일/매장에 연결되며 중복 요청을 재사용합니다. R2 원본 부재와 DB 실패를 성공으로 표시하지 않습니다.
- 기본 WebCrypto와 구형 환경용 SHA-256 구현을 Node 네이티브 해시와 여러 길이/경계에서 대조했습니다.
- 자정을 넘는 스케줄은 시작 요일 기준으로 Player/CMS가 일치합니다.
- 원본 대조 완료/미완료, 준비 중단, 다음 시간대 준비 상태가 UI에 구분됩니다.

## 시험 결과를 해석할 때
실패 주입 시험의 출력에는 의도된 offline/hash mismatch/quota 오류 스택이 포함됩니다. 각 테스트의 통과 여부와 마지막 집계를 기준으로 읽어 주세요.

초기 병렬 시험에서는 가상 시계가 실제 비동기 해시 작업보다 먼저 진행되어 시작 상태를 너무 일찍 검사한 사례가 있었습니다. 시험 파일을 순차 실행하고 앱 모형의 시간 진행에 비동기 작업 처리 시간을 두어 재현성을 보완했습니다. 실패 조건을 삭제하거나 통과 기준을 완화하지 않았습니다.

## 미검증·제한 범위
- 72시간은 가속된 가상 시간 시험입니다. 실제 TV 72시간 운전 결과가 아닙니다. 비디오 디코더 할당과 프레임 진행은 가상 미디어 요소로 검증합니다.
- 실제 브라우저 접근은 실행 환경 정책에 의해 차단되어 실제 브라우저 렌더링·클릭·코덱 재생을 검증하지 못했습니다. UI 시험은 DOM 모형의 로직/출력 검사입니다.
- CMS DB는 실제 로컬 SQLite를 D1 인터페이스로 연결해 시험했습니다. 운영 Cloudflare D1/R2/Pages 배포·권한·요금제·부하 검증이 아닙니다. R2 입출력은 모형을 사용합니다.
- Android 앱 소스/실제 문제 영상/장비가 제공되지 않았으므로 APK 변경, 실제 동시 디코드, HDMI 출력, 전원 재시작, 장비 저장장치 수명/발열과 네이티브 감시는 검증하지 않았습니다.
- SHA-256 일치는 파일의 원본 일치 여부이며, 정상 디코드나 광고 도달/시청자를 보증하지 않습니다. 기존 원본 해시가 없는 파일은 로컬 비교 수준이며 별도로 표시됩니다.
- 별도 보안 침투 시험과 500대 부하 시험, 자동 트랜스코딩/전체 영상 디코드 검사는 수행하지 않았습니다.

DEPLOY_AND_OPERATIONS_KO.md의 실제 장비 검증을 거친 뒤 단계적으로 확대 배포하세요.
