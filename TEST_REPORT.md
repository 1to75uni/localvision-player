# Player 1.9.1 시험 보고서

시험일: 2026-09-10. 실행 환경: Linux / Node.js v24.19.0.
대상: 이 ZIP에 포함된 Player 소스. CMS 2.1.0과 Android APK는 이번에 변경하지 않았습니다.

## 결과

| 검사 | 결과 |
| --- | --- |
| Player 전체 자동 회귀 시험 | 62개 통과, 실패 0, 취소 0, 건너뜀 0 |
| 위 시험 중 서비스 워커 시험 | 16개 통과 |
| JS/CJS 문법 검사 | 10개 파일 통과 |
| HTML이 참조하는 파일 및 1.9.1 버전 일치 | 통과 |
| 재생·검증 핵심 모듈의 의도치 않은 변경 확인 | runtime.js, integrity.js, playlist-store.js는 1.9.0과 동일 |

완료된 전체 회귀 실행은 약 43.6초였습니다. 원시 결과는 `verification/player191-final.tap`, 정적 검사는 `verification/player191-static.json`에 있습니다. 시험 중 표시되는 네트워크·저장 실패 메시지는 장애를 주입하는 시험의 출력입니다. TAP 마지막의 전체 건수와 실패 수를 함께 확인하세요.

## 이번에 추가로 검증한 위험

- 1.9.0 페이지 응답 분기와 리디렉션 이력이 있는 저장 응답을 조합하면, 시험에 구현한 Fetch 표준 검사에서 네트워크 오류가 발생합니다. 이 이전 분기를 회귀 fixture로 보존했습니다.
- 1.9.1은 같은 조건의 응답을 새 Response로 정규화하여 그 검사를 통과합니다. 설치 시 저장 응답과 실제 페이지 이동 시 응답을 각각 검사했습니다.
- 해제된 압축/이전 전송 크기 헤더를 페이지 응답에 재사용하지 않는지 확인했습니다.
- 캐시 읽기 실패·캐시 누락·오프라인·HTTP 오류·HTML이 아닌 응답·다른 출처의 응답에 대한 처리를 검사했습니다.
- 실패한 설치가 skipWaiting으로 활성화되지 않는지, 캐시 정리 실패가 새 서비스 워커의 제어권 획득을 막지 않는지 검사했습니다.
- 이전 앱 화면 캐시만 삭제하고 미디어 캐시를 보존하는지 확인했습니다.
- API/버전 조회가 페이지 캐시로 대체되지 않는지와 정적 파일 캐시 쓰기 실패 시 정상 서버 응답을 반환하는지 검사했습니다.

## 기존 기능 회귀 시험

전체 62개에는 좌우 독립 재생 복구, 파일 SHA-256 대조, 손상된 파일의 개별 복구, 저장 공간 부족 시 현재 목록 보존, 목록 기록 복원, 다음 일정 준비 및 장시간 가속 재생 시험이 포함됩니다.

‘72시간’ 재생 시험은 가짜 시계를 사용하는 자동 시험입니다. 실제 기기를 72시간 연속 운전한 결과가 아닙니다.

## 정확한 검증 한계

이번 서비스 워커 시험은 Node VM, 응답/캐시 모형, Fetch 표준의 리디렉션 검사 규칙을 사용합니다. 실제 Chrome 또는 Android WebView의 서비스 워커를 실행한 시험이 아닙니다. 실제 Cloudflare 리디렉션의 네트워크 왕복도 이 시험에 포함하지 않습니다. 따라서 시험 통과만으로 사진 속 장비의 ERR_FAILED 원인이 확정되거나 모든 장비가 복구됐다고 판단할 수 없습니다.

현장에서는 1.9.1 배포와 기기 적용을 확인한 뒤, 같은 장비에서 브라우저/LocalVision 앱을 비교해야 합니다. 최초 로딩·반복 열기·네트워크 차단 후 열기·좌우 실제 영상 재생을 확인하세요. 문제가 계속되면 장비 모델, OS/WebView 버전, 발생 시각 및 앱 오류 로그가 필요합니다. APK, 하드웨어 디코더, HDMI, 매장 DNS·라우터 검증은 수행하지 않았습니다.

## 재현

프로젝트 루트에서 Node.js 24로 실행합니다. 추가 npm 의존성이 없습니다.

```sh
node --test --test-reporter=tap --test-concurrency=1 tests/*.test.cjs
```

시험 근거: [Cloudflare Pages의 HTML 주소 처리](https://developers.cloudflare.com/pages/configuration/serving-pages/), [Fetch 표준의 서비스 워커 응답 검사](https://fetch.spec.whatwg.org/#http-fetch).
