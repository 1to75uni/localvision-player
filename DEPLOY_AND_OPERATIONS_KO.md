# Player 1.9.6 — 이번 오류 수정 적용 안내

CMS 2.1.2 FREE 100을 유지하고 Player만 1.9.6으로 교체합니다. 아래 FREE 100 안내의 기존 Player 1.9.5는 이번 1.9.6으로 대체됩니다. CMS가 아직 2.1.2가 아니라면 이전에 제공한 CMS 2.1.2 패키지를 먼저 적용해야 합니다.

1. 이 ZIP을 풀고 최상위 폴더 안의 파일 전체를 Player GitHub 저장소 루트에 반영합니다. api-response.js, app.js, index.html, sw.js와 대기 사진을 포함해 전부 업로드하세요. ZIP 파일 자체만 저장소에 올리면 배포 소스로 적용되지 않습니다.
2. 기존 Cloudflare Pages 설정으로 배포합니다. 이 패키지는 완성된 정적 Player 소스입니다. 새 유료 서비스, DB 테이블 또는 새 바인딩을 추가하지 않습니다.
3. 기기를 한 번 다시 열어 새 웹 플레이어를 적용합니다. /version.json의 build가 v1.9.6-api-recovery인지 확인합니다. 화면을 빠르게 다섯 번 누르거나 기존 URL에 debug=1을 붙이면 Player 버전과 CMS 연결 진단을 확인할 수 있습니다.
4. 사이트 데이터나 미디어 캐시를 삭제하지 마세요. 서버 장애 중 저장된 영상으로 재생을 유지하는 데 필요합니다.
5. 먼저 두 기기에서 기존 영상, 좌우 대기 사진, 네트워크 차단/복구를 확인한 후 나머지 기기에 적용하세요. 이것은 현장 적용 절차이며 이번 자동시험에서 실제 TV를 사용했다는 뜻은 아닙니다.

## 장애 중 화면과 재연결

| 상황 | Player 동작 |
|---|---|
| 유효한 플레이리스트와 정상 미디어가 로컬에 저장됨 | 기존 콘텐츠 재생 유지 |
| 처음 접속한 브라우저처럼 저장된 콘텐츠가 없음 | 좌우 대기 사진 표시, 서버 재연결 대기 |
| JSON/HTML/텍스트에 D1 일일 한도 초과가 명시됨 | LV-D1-QUOTA, 기본 약 15~16분 공통 API 대기 |
| Workers 요청 한도 또는 Error 1027이 명시됨 | LV-WORKER-QUOTA로 별도 구분, 기본 약 15~16분 대기 |
| 원인이 불명확한 HTML/잘못된 JSON 또는 네트워크 장애 | HTTP 정보가 있으면 보존, 일반 장애로 구분, 약 1분부터 15분까지 대기 간격 증가 |
| 경로별 일일 요청 예산 소진 | 해당 경로는 다음 UTC 날짜까지 대기 |
| 정상 CMS 응답과 콘텐츠 준비가 완료됨 | 자동 재생, 이전 오류 화면 닫기 |
| 실제로 편성된 콘텐츠가 없음 | LV-PLAYLIST-EMPTY 설정 안내 유지 |

서버가 Retry-After를 보내면 더 긴 대기 시간을 존중하며 최대 24시간으로 제한합니다. 재연결 시도도 기존 일일 API 예산에 포함됩니다. 대기 중 수동 Sync, 하트비트, 로그 업로드가 서버를 반복 호출하지 않도록 공통 대기를 적용했습니다. 성공했던 영상·재생목록을 서버 오류 응답으로 덮어쓰지 않습니다. 별도 R2 미디어 접근은 CMS JSON API 일시 중지와 분리되어 있습니다.

진단에는 HTTP 상태, 응답 Content-Type, 가능할 경우 Cloudflare Ray ID, 재확인 시각이 남습니다. 브라우저가 CORS/통신 차단으로 응답 자체를 전달하지 않으면 HTTP 상태는 '응답 없음'입니다. 이 경우 D1인지 Workers인지 확정할 수 없습니다. 오류 상세는 로컬 대기열에 보관하며 CMS 회복 후 기존 로그 전송·표본 제한 안에서 전달됩니다.

이 수정은 D1 한도 자체를 해제하거나 사용량을 초기화하지 않습니다. D1 일일 한도는 UTC 00:00(한국시간 오전 9시)에 초기화됩니다. 서버에서 아직 받지 못한 영상을 새 기기가 재생하게 만드는 기능도 아닙니다. 같은 매장 URL이라도 휴대전화 Chrome/Safari와 TV 앱은 저장 공간이 달라, TV에 있는 파일을 새 휴대전화 브라우저가 곧바로 사용할 수는 없습니다. 앱/웹 페이지 자체가 열리지 않는 장애는 별도입니다.

공식 근거: [D1 일일 한도 및 초기화](https://developers.cloudflare.com/d1/platform/pricing/), [Workers 제한](https://developers.cloudflare.com/workers/platform/limits/)

---

# LocalVision FREE 100 — 배포 안내

CMS 2.1.2와 Player 1.9.5를 함께 적용하는 전체 소스 패키지입니다. 결제, GitHub push, Cloudflare 배포는 수행하지 않았습니다. 새 유료 서비스나 새 바인딩은 필요하지 않습니다.

## 배포
1. 기존 저장소 커밋과 CMS 설정을 백업합니다. 기기의 미디어 캐시는 삭제하지 마세요.
2. CMS ZIP 최상위 폴더 안 파일을 CMS 저장소 루트에 반영합니다. functions 폴더가 루트에 있어야 합니다. Pages 빌드 `node build-static.js`, 출력 `dist`, DB=D1 및 MEDIA=R2 등 기존 바인딩을 유지합니다.
3. 기존에 직접 지정한 경우 D1_HEARTBEAT_WRITE_SEC=600, ONLINE_TTL_SEC=1500으로 맞춥니다. 미지정 시 이 값이 기본입니다. 기존 값이 더 크면 실제 저장/판정 시간이 길어질 수 있습니다.
4. CMS 배포 완료 후 Player ZIP 최상위 폴더 안 파일을 Player 저장소 루트에 반영합니다. free-budget.js, waiting-left.jpg, waiting-right.jpg를 포함해 전부 반영해야 합니다. CMS의 /api/player-control 응답이 먼저 제공되어야 합니다.
5. 기존 URL을 유지해도 너무 짧은 폴링 간격은 Player가 최소값으로 보정합니다. 명령·공지·휴무 5분, 하트비트 10분, 편성 15분, app-config 30분이 최소값입니다. URL이 더 긴 주기를 지정하면 그 값을 유지합니다. 0은 해당 정기 조회 비활성화입니다.
6. 2대에서 네트워크 단절/복구, 재부팅, 좌우 동시 영상과 대기 이미지를 확인하고 하루 사용량을 관찰합니다. 이후 10대→전체 설치 기기→100대 순서로 확대합니다. 기존 커밋으로 되돌리면 롤백할 수 있지만 과다 쓰기 경로도 복귀할 수 있습니다.

## 구현된 절약 및 복구 정책
- 접속 기록은 최소 10분 간격 저장, 상세 상태는 최소 15분 간격 저장입니다. 하트비트가 10분이면 상세 저장은 보통 20분 간격입니다. 앱/부팅/재생 상태 변화로 저장 간격을 우회하지 않습니다.
- 온라인 기준은 기본 25분입니다. 표시된 수신 시각은 현재 화면의 실시간 증명이 아닙니다.
- 명령/공지/휴무를 읽기 전용 player-control 한 HTTP 요청으로 묶었습니다. 개별 조회 실패가 다른 성공 결과를 없애지 않습니다. 휴무 조회 오류를 정상 OFF로 처리하지 않습니다.
- 동일한 정상 플레이리스트 확인 로그는 D1로 보내지 않습니다. 새 상세 오류는 매장당 UTC 하루 최대 48건 저장하며 초과분은 표본 제한으로 명시합니다. 전체 오류 무제한 보관 기능은 아닙니다. 현재 제외 파일과 오류 집계는 상세 상태에 별도 포함됩니다.
- Player CMS JSON API의 실제 시도 횟수(실패 포함)를 로컬 저장소에 기록합니다. 기기·매장 프로필당 UTC 하루 control 310, state 110, heartbeat 160, app-config 55, 상세 상태 100, 로그 50, 그 밖의 API 20회로 제한합니다. 경로별 예산을 따로 적용하여 오류 로그가 하트비트 예산을 소진하지 않습니다.
- 경로별 예산 소진 시 해당 요청은 다음 UTC 날짜까지 대기합니다. 저장된 영상은 계속 재생할 수 있지만 새 명령/편성/상태 보고가 지연될 수 있습니다. 재시작으로 예산을 초기화하지 않습니다. 여러 탭/플레이어를 같은 TV에서 중복 실행하지 말고 저장소를 반복 삭제하지 마세요.
- D1 한도 오류 시 기존 약 15~16분 재시도 대기와 검증 캐시 재생을 유지합니다. 첫 설치로 캐시가 없거나 앱/페이지 자체를 열 수 없는 상태까지 재생을 보장하지는 않습니다.

## 100대 계산 결과와 무료 운영 조건
가정: 고정 ID의 TV 100대, 독립 매장 프로필 100개, 24시간 동작, 기본 스키마/인덱스, 변경하지 않은 보호 설정.

| 항목 | 하루 실제 SQLite 변경 행 | 보수적 인덱스 배수 | 추정 D1 쓰기 행 |
|---|---:|---:|---:|
| 접속 상태 | 14,400 | 2 | 28,800 |
| 상세 상태 | 9,600 | 3 | 28,800 |
| 오류 표본 | 4,800 | 4 | 19,200 |
| 합계 | 28,800 | — | 76,800 |

무료 100,000 쓰기 행/일 대비 23,200행이 다른 작업의 여유입니다. 이것은 보수적 모델이고 Cloudflare 실제 과금 행 측정은 아닙니다. 초기 설치, 인덱스 추가, 관리자 작업, 다른 DB/앱은 별도입니다.

Player가 관리하는 CMS JSON 요청 상한은 100대 합계 80,500회/일입니다. Workers/Pages Functions 무료 요청 한도 100,000회/일과는 다른 범위입니다. 나머지 19,500회 안에 네이티브 APP 자체 요청, 미디어 다운로드/검증 API, CMS 관리자 및 다른 Worker 요청이 들어와야 합니다. 예를 들어 APP 자체 요청이 TV당 10분에 1회면 14,400회/일이 추가되어 94,900회가 되고 기타 작업 여유는 5,100회입니다. APK 소스가 없어 실제 APP 자체 호출량은 검증하지 못했습니다. 여러 API를 별도로 호출하면 반드시 실제 사용량으로 다시 계산해야 합니다.

읽기 무료 한도 5,000,000행/일 및 D1 저장 공간 5GB, R2 Standard 무료 저장량 10GB-month와 작업량 한도 등도 별도 조건입니다. TV 수만으로 전체 무료를 보장할 수 없습니다. 유료 플랜으로 자동 전환하거나 결제를 설정하는 코드는 없습니다. 정상 운용일과 대량 콘텐츠 교체일 모두 확인하세요.

수동 측정: CMS `/api/capacity-check?store=매장코드`는 player-state와 player-control을 각각 한 번 읽어 실제 D1 meta 값을 보여줍니다. 자동 폴링하지 마세요. 각 TV의 rowsRead × 하루 호출 횟수를 합산하고 하트비트/상태/로그 및 관리자 조회량을 더해 계정 실제 Rows read와 비교합니다. 다른 매장의 편성 수와 인덱스가 다르면 읽기량도 다릅니다.

권장 확대 조건: 계정 전체 일일 쓰기 90,000행 이하, 읽기 3,500,000행 이하, 동적 요청 90,000회 이하를 정상 운영 목표로 삼고 남는 여유를 업데이트/장애에 사용합니다. 이 수치는 운영 목표이며 계정 전체 사용량을 강제로 차단하는 기능은 아닙니다.

기본 48건 오류 기록이 계속 쌓이므로 장기 보관량도 점검하세요. 기록 삭제 역시 D1 쓰기를 소비하므로 대량 일괄 삭제를 피하고 백업 후 관리합니다. 100대를 넘어선 확장은 다시 측정해야 합니다.

## 검증 범위
Node/SQLite/가상 시계·미디어 회귀시험입니다. 실제 TV 100대, Cloudflare 전체 계정, 장기 디코더 부하, 실제 읽기/과금은 측정하지 않았습니다. 양쪽 테스트 원문은 각 ZIP의 verification/current-tests.tap에 있습니다.

공식 기준: https://developers.cloudflare.com/d1/platform/pricing/ 및 https://developers.cloudflare.com/workers/platform/pricing/ 및 https://developers.cloudflare.com/r2/pricing/
