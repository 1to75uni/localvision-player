{
  "build": "v1.6.10-cors-safe-fetch",
  "version": "1.6.10",
  "updatedAt": "2026-05-08",
  "highlights": [
    "Removed custom cache-control request header from CMS API fetch to avoid CORS preflight failures",
    "Uses cache: no-store plus timestamp query cache busters instead of custom request headers",
    "Error screen now includes endpoint, URL, HTTP status, and boot step for diagnosis",
    "Service Worker cache name bumped to v1.6.10 to clear older player app cache",
    "Compatible with CMS v1.7.8 CORS SAFE + UI POLISH and APP v9.2 Minimal Shell"
  ],
  "notes": "CORS-safe fetch build for cross-domain Player -> CMS API calls."
}
